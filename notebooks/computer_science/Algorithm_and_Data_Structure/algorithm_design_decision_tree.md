# Algorithm Design Decision Tree  
## —— 何时先建模，何时先构造过程，何时必须数学化

> 目的：  
> 当面对一个新问题时，快速判断  
> - 是否可以像 KT 一样「算法即证明」  
> - 是否必须先写数学模型  
> - 应该优先选择哪一类算法范式

---

## Step 0：问题原型判断（第一分叉）

### Q0：问题的核心解是什么？

- A. 一个**结构**（匹配 / 路径 / 集合 / 划分）
- B. 一个**数值最优解**（最小值 / 最大值 / 期望）

👉  
- A → 进入 **结构型问题路线**
- B → 进入 **数值型问题路线**

---

## Part A：结构型问题路线（KT 主战场）

### Step 1：问题是否是有限离散的？

判断标准：
- 输入规模有限
- 状态空间可枚举（哪怕很大）
- 约束是逻辑谓词（是 / 否）

✅ 是 → 继续  
❌ 否 → 转 Part B（数值型）

---

### Step 2：解是否只需满足“性质”，而非“最优值”？

例如：
- 是否稳定
- 是否可行
- 是否无冲突
- 是否存在

✅ 是 → 继续  
❌ 否（仍然要最优）→ 可能是 DP / Flow / LP

---

### Step 3：是否存在“自然演化过程直觉”？

问自己一句话：

> 我能不能想象一个**系统在不断调整，  
> 错误逐渐被消除，  
> 最终停在一个好状态**？

常见直觉来源：
- 社会过程（匹配、博弈）
- 物理隐喻（流、扩散、切割）
- 交换 / 推进 / 淘汰

✅ 是 → **KT 构造性过程路线**  
❌ 否 → 跳到 Step 5

---

### Step 4：KT 构造性过程路线（Algorithm = Proof）

此时采用：

> **Process-based Design（过程即证明）**

标准操作模板：

1. 定义一个**局部、安全、可逆/延迟**的动作  
2. 引入一个或多个**单调不变式**
3. 证明过程：
   - 不会死循环
   - 必然终止
4. 证明终态：
   - 满足问题性质
   - 不可能被违反

📌 典型问题：
- Stable Matching
- Network Flow
- Min-Cut
- Bipartite Matching

⚠️ 注意：
- 数学模型已内化在定义中
- 不必显式写目标函数

---

### Step 5：若无自然过程 → 结构优化类问题

此时问题仍是结构性的，但：

- 目标是最优
- 无明显“自收敛过程”

👉 进入经典算法范式选择：

| 特征 | 优先考虑 |
|----|----|
| 最优子结构 | Dynamic Programming |
| 局部最优可扩展 | Greedy |
| 全局约束 | Flow / Matching |
| 决策树 | Backtracking / Search |

---

## Part B：数值型问题路线（必须显式建模）

### Step 6：是否存在明确目标函数？

例如：
- 最小化误差
- 最大化收益
- 最小能量

❌ 没有 → 问题本身不完整  
✅ 有 → 必须先数学建模

---

### Step 7：变量是否连续？

- 连续 → 连续优化 / 数值方法
- 离散 → ILP / CSP / SAT / DP

📌 在这里：
- **formulate ≠ 计算模型**
- 算法选择仍未确定

---

### Step 8：数学模型 → 算法选择

流程固定为：

> 现实 → 数学模型 → 数值/搜索算法 → 近似解

算法只是：
- 求解器
- 近似器
- 优化器

❗ 不可跳过建模步骤

---

## Step 9：一个总判断公式（强烈推荐记住）

> 如果一个问题：
> 1. 解是结构而非数值  
> 2. 约束是逻辑/关系性的  
> 3. 存在自然的演化直觉  
>
> 👉 **优先构造过程，用算法理解问题**

> 如果一个问题：
> 1. 解是数值最优  
> 2. 依赖连续变量或误差  
>
> 👉 **必须先写数学模型**

---

## Step 10：一句“防误用”警告（重要）

> ❌ 算法 ≠ 永远代替模型  
> ✅ 正确说法是：  
>
> **在结构性离散问题中，  
> 模型已在定义中，  
> 算法是对模型结构的探索。**

---

## Step 11：最终心智模型（给自己）

- DP / LP / 优化 → **先模型**
- KT / Flow / Matching → **先过程**
- NP 完全 → **先形式化，再讨论不可解性**

---

## 一句话收尾

> **不要急着写算法。  
> 先判断：  
> 你是在设计一个“会变正确的系统”，  
> 还是在数值空间里找最优点。**